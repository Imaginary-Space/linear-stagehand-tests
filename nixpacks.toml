# Nixpacks configuration
# See: https://nixpacks.com/docs/configuration/file

[phases.setup]
nixPkgs = ["nodejs_20", "bun"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["bun run build"]

[start]
cmd = "bun run start"
